[pytest]
testpaths = tests
# tests/ is on sys.path so conftest helpers import by name; the repo root is
# there too so `import nifty_algo` resolves without an install step.
pythonpath = . tests
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning
