# Core
pandas>=2.2
numpy>=1.26
scipy>=1.13          # Black-Scholes normal CDF

# UI
# 1.49+ for st.fragment(run_every=...), which replaced the third-party
# streamlit-autorefresh, and for width="stretch".
streamlit>=1.49
plotly>=5.22

# Data
kiteconnect>=5.0     # Zerodha: live quotes, real option chain, deep index history
yfinance>=0.2.40     # delayed fallback feed
pyarrow>=16.0        # parquet
requests>=2.32
python-dotenv>=1.0

# Alerts
plyer>=2.1           # Windows desktop toast

# Tests
pytest>=8.0
